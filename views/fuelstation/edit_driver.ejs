<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Driver</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/fuelstation/edit_driver.css">
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
</head>
<body>
    <!-- Sidebar -->
    <%- include('sidebar.ejs') %>

    <!-- Main Content -->
    <main class="main-content">
        <div class="dashboard-card">
            <h2 class="card-title"><ion-icon name="create"></ion-icon> Edit Driver Details</h2>
            
            <form action="/fuelstation/drivers/<%= driver._id %>/edit" method="POST" class="dashboard-form">
                <div class="form-grid">
                    <div class="form-group">
                        <label for="name">Driver Name</label>
                        <input type="text" id="name" name="name" value="<%= driver.userId.name %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" value="<%= driver.userId.email %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="phone">Phone</label>
                        <input type="text" id="phone" name="phone" value="<%= driver.userId.phone %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="aadhar">Aadhar Number</label>
                        <input type="text" id="aadhar" name="aadharNo" value="<%= driver.aadharNo %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="licence">Driving Licence</label>
                        <input type="text" id="licence" name="licenceNo" value="<%= driver.licenceNo %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="password">New Password (Leave blank to keep unchanged)</label>
                        <input type="password" id="password" name="password">
                    </div>
                </div>

                <input type="hidden" name="fuelStationId" value="<%= driver.fuelStationId %>">
                
                <button type="submit" class="btn-submit">
                    <ion-icon name="save"></ion-icon> Update Driver
                </button>
            </form>
        </div>
    </main>
</body>
</html>
